# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# Environment variables (but NOT .env.local.example)
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Convex
.convex

# Note: This .gitignore is for projects/alpha only
# It allows src/ and other source files to be tracked
# The root .gitignore excludes src/ but we override it here
.vercel
